[package]
name = "operune-domain"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Operune Core domain: pure domain types, state machines, invariants, compatibility rules (spec 24.2)"

[dependencies]

[lints]
workspace = true
