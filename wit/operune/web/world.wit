/// operune:web@0.1.0 — 参考 world（作者侧便利，非运行时事实源）
///
/// 携带 Web UI 的 Component 在 0.1.0 需要导出的 operune 契约总览。
/// Core 的兼容判断只依赖二进制中真实可观察的 exports（§6.7），
/// 不依赖 root world 名称。
package operune:web@0.1.0;

/// 带 Web UI 的 Component：必须同时导出 operune:component/descriptor
/// （安装 / 身份契约）与 operune:web 的三个契约。
///
/// 注意：0.1.0 的 operune:web 契约全部为 Component 导出（export），
/// Core 是这些导出的唯一消费者（Host 侧）。WIT 中不存在
/// "Core 向 Component 提供 Web 能力" 的 import 方向：
/// 静态资产由 Core 缓存直供浏览器，action 由 Core-mediated bridge
/// 转发（§21.3），组件不自行 bind 监听端口。
world operune-web-component {
    export operune:component/descriptor@0.1.0;
    export descriptor;
    export assets;
    export actions;
}
